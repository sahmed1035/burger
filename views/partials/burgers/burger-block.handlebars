<li>
	{{burger_name}}

	<button class="change-devour btn btn-warning" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}Devour It!{{else}}Devoured!!{{/if}}
	</button>

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</li>
